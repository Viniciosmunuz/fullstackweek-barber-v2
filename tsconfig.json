{
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "//exclude": "tests/ fica fora do typecheck do next build de proposito: os testes importam o vitest, que e dependencia de desenvolvimento e pode nao estar instalada no ambiente de build. Sem isso, um deploy quebraria por causa de um import que nao afeta o site. O vitest transpila os arquivos por conta propria e nao depende deste include.",
  "exclude": ["node_modules", "tests"]
}
